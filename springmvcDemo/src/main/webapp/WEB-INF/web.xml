<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
                      http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd"
         version="3.1"
>
    <!--注册一个前端控制器
        DispatcherServlet
    -->
    <servlet>
        <!--这里写的这个名字是很有讲究的
        如果我们不去修改spring配置文件的默认位置，
        它会去WEB-INF下面找一个叫"XXXXX-servlet.xml"的文件
        这里的XXXXX代表servlet-name里注册的名字。即springmvc-servlet.xml.
        -->
        <servlet-name>springmvc</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <!--初始化命名
        namespace是命名空间，param-value的值为配置springmvc的"XXXX.xml"文件名
        即名字"mvc-servlet"
        这里默认查找的位置是WEB-INF下。-->
        <!--<init-param>-->
            <!--<param-name>namespace</param-name>-->
            <!--<param-value>mvc-servlet</param-value>-->
        <!--</init-param>-->
        <!--上下文配置的位置制定-->
        <init-param>
            <param-name>contextConfigLocation</param-name>
            <param-value>classpath:springmvc.xml</param-value>
        </init-param>
        <!--Servlet3.0新特性配置上传文件大小配置信息-->
        <!--<multipart-config>-->
            <!--<max-file-size>200000000</max-file-size>-->
        <!--</multipart-config>-->
        <!--Servlet3.0新特性配置上传文件大小配置信息==end-->
    </servlet>
    <!--servlet映射配置-->
    <servlet-mapping>
        <servlet-name>springmvc</servlet-name>
        <!--这里统一给我写"/"-->
        <!--<url-pattern>*.do</url-pattern>-->
        <url-pattern>/</url-pattern>
    </servlet-mapping>
    <!--处理post中文编码问题,建议把它放在所有过滤器最前面-->
    <filter>
        <filter-name>characterEncodingFilter</filter-name>
        <filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
        <init-param>
            <param-name>forceRequestEncoding</param-name>
            <param-value>true</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>characterEncodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>
    <!--//注册一个支持所有的http请求类型的过滤器-->
    <filter>
        <filter-name>hiddenHttpMethodFilter</filter-name>
        <filter-class>org.springframework.web.filter.HiddenHttpMethodFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>hiddenHttpMethodFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>
</web-app>
